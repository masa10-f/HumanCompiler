{
  "name": "@task-agent/scheduler",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.py",
  "scripts": {
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format ."
  },
  "dependencies": {
    "ortools": ">=9.8.0",
    "pydantic": ">=2.5.0"
  },
  "devDependencies": {
    "pytest": ">=7.4.0",
    "ruff": ">=0.1.0"
  }
}